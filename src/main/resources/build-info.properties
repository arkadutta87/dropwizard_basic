build.version=${project.version}
build.date=${timestamp}
group.id=${project.groupId}
artifact.id=${project.artifactId}
scm.tag=${project.scm.tag}
build.branch=${buildScmBranch}
build.number=${buildNumber}